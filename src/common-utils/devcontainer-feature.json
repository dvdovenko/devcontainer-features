{
  "name": "Common Utilities",
  "id": "common-utils",
  "version": "1.0.4",
  "description": "Installs common useful utilities for development",
  "installsAfter": [
      "ghcr.io/devcontainers/features/common-utils",
      "ghcr.io/devcontainers/features/git",
      "ghcr.io/devcontainers-extra/features/eza"
  ],
  "options": {
    "installNeovim": {
      "type": "boolean",
      "default": true,
      "description": "Install Neovim (set to false to skip)"
    },
    "installWatchman": {
      "type": "boolean",
      "default": true,
      "description": "Install Watchman (set to false to skip)"
    },
    "installOpenCode": {
      "type": "boolean",
      "default": false,
      "description": "Install OpenCode (set to true to install)"
    },
    "installCodex": {
      "type": "boolean",
      "default": false,
      "description": "Install Codex (set to true to install)"
    }
  }
}